import cookie from "cookie";

// export default async function handler(req, res){

//   res.setHeader('Set-Cookie', [
//     serialize('userData', '', {
//       maxAge: -1,
//       path: '/',
//     }),
//   ]);

//   res.writeHead(302, { Location: '/api/singin' });
//   res.end();
// };